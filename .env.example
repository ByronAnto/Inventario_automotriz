##############################################################
# AutoPartes Inventory - Supabase Self-Hosted Environment
# INSTRUCCIONES: Copiar este archivo como .env y completar los valores
#   cp .env.example .env
##############################################################

# ── Secrets (CAMBIAR en producción) ──────────────────────────
POSTGRES_PASSWORD=CambiarEstaPassword123!
JWT_SECRET=cambiar-este-jwt-secret-con-al-menos-32-caracteres
PG_META_CRYPTO_KEY=cambiar-esta-crypto-key-al-menos-32-chars!

# ── JWT Tokens ───────────────────────────────────────────────
# Generar en https://jwt.io con tu JWT_SECRET
# ANON_KEY payload:         {"role":"anon","iss":"supabase","iat":1700000000,"exp":2000000000}
# SERVICE_ROLE_KEY payload: {"role":"service_role","iss":"supabase","iat":1700000000,"exp":2000000000}
ANON_KEY=PEGAR_AQUI_TU_ANON_KEY_GENERADO
SERVICE_ROLE_KEY=PEGAR_AQUI_TU_SERVICE_ROLE_KEY_GENERADO

# ── Puertos ──────────────────────────────────────────────────
POSTGRES_PORT=5434
KONG_HTTP_PORT=8000
STUDIO_PORT=3100
META_PORT=8090
GOTRUE_PORT=9999
REALTIME_PORT=4000
REST_PORT=3200
STORAGE_PORT=5000
IMGPROXY_PORT=5001

# ── Frontend Web (contenedor Nginx) ──────────────────────────
WEB_PORT=3000

# ── URLs (cambiar localhost por tu IP/dominio en producción) ─
SITE_URL=http://localhost:3100
API_EXTERNAL_URL=http://localhost:8000
SUPABASE_PUBLIC_URL=http://localhost:8000

# ── Auth (GoTrue) ────────────────────────────────────────────
GOTRUE_SITE_URL=http://localhost:3100
GOTRUE_EXTERNAL_EMAIL_ENABLED=true
GOTRUE_MAILER_AUTOCONFIRM=true
GOTRUE_SMS_AUTOCONFIRM=true
GOTRUE_DISABLE_SIGNUP=false
GOTRUE_ENABLE_SIGNUP=true

# ── Database ─────────────────────────────────────────────────
POSTGRES_HOST=supabase-db
POSTGRES_DB=postgres
POSTGRES_USER=postgres

# ── Studio ───────────────────────────────────────────────────
STUDIO_DEFAULT_ORGANIZATION=AutoPartes
STUDIO_DEFAULT_PROJECT=Inventario Automotriz

# ── Usuario Admin (se crea automáticamente con init-schema.sh) ─
ADMIN_EMAIL=admin@autopartes.com
ADMIN_PASSWORD=Admin2026!
ADMIN_NOMBRE=Administrador
